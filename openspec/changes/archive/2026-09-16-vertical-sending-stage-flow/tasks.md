## 1. 发送阶段视图

- [x] 1.1 将 `SecureFlow.vue` 从横向五节点渲染改为当前阶段单卡渲染，复用 `secureSteps.ts` 的图标、标题与说明；检查代码中只显示一个阶段卡。
- [x] 1.2 为阶段切换添加小型五点状态指示，并保留当前线性进度条；检查发送中不出现重复的阶段标题/说明块。
- [x] 1.3 更换 `InferenceView.vue` 发送中区域布局为 `SecureFlow` 单卡视图；检查阶段卡到响应调试/结果区之间没有旧横向节点残留。
- [x] 1.4 在全局主题中定义固定占位高度、当前阶段样式和垂直转场样式；定义减少动态效果回退；检查 CSS 无依赖新增库。

## 2. 文档同步

- [x] 2.1 更新 `docs/tngui-ui-guide.md` 请求调试/SecureFlow 描述为垂直单卡转场；验证文档不再把发送动画描述为横向五节点。

## 3. 验证

- [x] 3.1 运行 `npm test`、`npm run typecheck` 和 `npm run build`；全部通过。
- [x] 3.2 用低矮宽度视口或人工窄窗口模拟发送，验证五个阶段切换方向、固定高度、无横向溢出与最终响应展示。
- [x] 3.3 在减少动态效果偏好下验证阶段可直接切换且不执行位移动画。
- [x] 3.4 运行 `openspec validate --strict`；验证本 change 的 delta spec 可解析且无占位错误。
