## 1. 窗口与发布版本

- [ ] 1.1 在 `tauri.conf.json` 主窗口设置启动最大化，并保留 1440×960 还原基准与 1120×720 最小尺寸；确认不使用 fullscreen，并可通过运行 GUI 观察到启动即最大化、还原后不小于最小尺寸。
- [x] 1.2 确认 workspace `Cargo.toml` 版本保持 `0.4.1`、未重复 bump；启动客户端信息页展示的版本仍是同一编译期版本。

## 2. 密态推理功能卡布局

- [ ] 2.1 为 `feature-card` 增加局部标题区样式（`min-height >= 66px`、图标/标题/副标题垂直居中），确认不改变全局 `.ant-card-head` 或其他卡片；用浏览器开发工具检查实际渲染尺寸与对齐。
- [ ] 2.2 收缩 `feature-card` 内容区 API Key 行上下间距，并将 API Key 输入从大号尺寸改为默认/普通行高；确认上下额外间距均不超过 12px 且卡片内容不被截断。
- [ ] 2.3 打开设置页对照入口编辑器、客户端信息卡与其他卡片，确认它们未受本布局调整影响。

## 3. ingress 默认远端端口

- [x] 3.1 在 `formspec.ts` 建立 mapping `80` 与 http_proxy `443` 两个模式专属默认远端端口，并更新默认模板和远端类型切换路径；确认反代对外端口仍保持 `9443`。
- [x] 3.2 更新配置回填/解析中缺失端口的默认值，同时保留 JSON 中显式端口（包括显式 `0` 和非默认值）；检查导入、表单切换与序列化不覆盖用户端口。
- [x] 3.3 更新/新增 Vitest 断言：默认模板 mapping 端口为 80、切换 http_proxy 默认 443、切换回 mapping 默认 80、显式端口不回写默认值；运行 `npm test` 验证。

## 4. 文本输入禁用自动改写

- [x] 4.1 为 API Key、远端 IP/域名、verify、model、prompt 和原始 JSON 编辑器涉及的文本控件添加 `autocapitalize="off"`、`autocorrect="off"` 和禁用拼写检查属性；确认属性落在实际 `<input>/<textarea>` 上。
- [ ] 4.2 在 macOS 构建中分别输入小写开头的英文域名、model、prompt 和 JSON；确认首字母不再被自动变大写，粘贴内容大小写不变。
- [ ] 4.3 在其他桌面平台打开同一页面，确认这些属性不引起布局、控制校验或 JSON 语义变化。

## 5. 整体验证

- [x] 5.1 运行前端 `npm test` 和 `npm run typecheck`，确认默认端口、配置序列化与新 UI 行为测试通过。
- [ ] 5.2 运行 `cargo check`（或项目常规 Rust 校验），确认 Tauri 配置读取与版本状态无编译问题。
- [ ] 5.3 执行桌面烟测：启动即窗口最大化、设置页“密态推理”卡留白收紧、mapping/http_proxy 切换默认端口符合预期、macOS 英文首字母保持小写。
