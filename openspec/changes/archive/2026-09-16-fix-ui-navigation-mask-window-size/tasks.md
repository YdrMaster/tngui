## 1. 导航与误导性状态行

- [x] 1.1 将 `InferenceView.vue` 的“前往设置”按钮改为调用注入的 `navigate("settings")`，并移除该按钮的 `message.info`；验证在不可发送状态下点击后右侧视图变为“设置”，无导航提示弹窗。
- [x] 1.2 从 `IngressInfoCard.vue` 移除灰色圆点和“仅配置摘要”状态行；验证概览入口信息卡直接显示“入口模式 / 绑定地址 / 本机端口”，且不出现状态点或健康语义。

## 2. API Key 遮罩

- [x] 2.1 将设置页 API Key 输入从普通 `a-input` 改为密码式遮盖输入并保留显隐切换；验证默认显示遮罩、点击切换后显示真实值，且没有新增导入导出或验证控件。
- [x] 2.2 检查 `useTngConfig.serializeCurrent()` 与相关测试输出；验证 apiKey/model 仍不入 `tng-runtime.json`，切换显隐不会标记配置 dirty。

## 3. 窗口尺寸与紧凑布局

- [x] 3.1 更新 `tauri.conf.json` 桌面窗口为默认 1440×960、最小 1120×720；验证配置文件中四个值及其逻辑像素语义与规格一致。
- [x] 3.2 调整全局主题与外层布局：将全局内容最小宽度降到 1120px，压缩侧边栏与内容区留白；在 1120×720 逻辑视口下确认无全局横向滚动。
- [x] 3.3 压缩 `EntryEditor.vue`、`FieldRenderer.vue` 中远端类型、远端字段、端口和 verify 控件的固定/最小宽度；验证 mapping 与 http_proxy 两类表单在默认窗口和最小窗口下都完整可输入。
- [x] 3.4 降低 JSON 编辑器、响应/空态/展示面板等非交互区最小高度，并整理页面间距；验证概览默认窗口内四卡和调试面板可读，设置页主要控件未横向截断。

## 4. 文档与验收

- [x] 4.1 更新 `docs/tngui-ui-guide.md` 中“前往设置”、API Key 遮罩、默认窗口尺寸和入口信息卡的描述；确认文档不含旧“普通文本、不遮码”表述。
- [x] 4.2 运行 `npm test`、`npm run typecheck` 和 `npm run build`；全部通过后确认无既有配置序列化测试回归。
- [x] 4.3 运行 `openspec validate --strict`；验证变更中的 ADDED/MODIFIED requirement 可被解析且无占位错误。
- [ ] 4.4 在桌面运行态做最终视觉验证：首开为 1440×960，缩小到 1120×720 仍无横向滚动，设置页 API Key 默认遮盖，密态推理页与概览页“前往设置”均直接跳转。
- [x] 4.5 提交时保留本 OpenSpec 变更目录中的第四项 UI 修正范围；验证提交内容不含无关后端或启动逻辑改动。
